#include "main.h"
/**
 * _abs - returns the absolute value of an integer
 *
 * @r: integer
 *
 * Return: the absolute value
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}

}
